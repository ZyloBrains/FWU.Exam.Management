@page
@model ResendEmailConfirmationModel
@{
    ViewData["Title"] = "Resend email confirmation";
}

<div class="max-w-lg mx-auto mt-10">
    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8">
        <div class="flex items-center gap-3 mb-6">
            <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center">
                <i class="fas fa-envelope text-blue-600"></i>
            </div>
            <div>
                <h1 class="text-xl font-bold text-gray-800">@ViewData["Title"]</h1>
                <p class="text-sm text-gray-500">Enter your email.</p>
            </div>
        </div>
        <form method="post" class="space-y-5">
            <div asp-validation-summary="All" class="text-sm text-red-600 bg-red-50 rounded-lg p-3"></div>
            <div>
                <label asp-for="Input.Email" class="block text-sm font-semibold text-gray-700 mb-2"></label>
                <input asp-for="Input.Email" class="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors" aria-required="true" placeholder="name@example.com" />
                <span asp-validation-for="Input.Email" class="text-xs text-red-500 mt-1 block"></span>
            </div>
            <button type="submit" class="w-full px-6 py-3 bg-blue-600 text-white text-sm font-semibold rounded-lg hover:bg-blue-700 transition-colors shadow-sm">
                <i class="fas fa-paper-plane mr-1"></i> Resend
            </button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
