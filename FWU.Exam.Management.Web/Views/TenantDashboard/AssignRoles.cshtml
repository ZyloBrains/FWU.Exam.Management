@model AssignRolesViewModel

@{
    var tenant = ViewBag.Tenant as Tenant;
    ViewData["Title"] = $"Assign Roles — {tenant!.Name}";
}

<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="TenantDashboard" asp-action="Index" asp-route-tenantCode="@tenant.OfficeCode">@tenant.Name</a></li>
        <li class="breadcrumb-item"><a asp-controller="TenantDashboard" asp-action="Users" asp-route-tenantCode="@tenant.OfficeCode">Users</a></li>
        <li class="breadcrumb-item active">Assign Roles</li>
    </ol>
</nav>

<h1 class="h3 mb-1">Assign Roles</h1>
<h5 class="text-muted mb-4">@Model.UserEmail</h5>

<div class="row">
    <div class="col-md-4">
        <form asp-controller="TenantDashboard" asp-action="AssignRoles"
              asp-route-tenantCode="@tenant.OfficeCode" asp-route-userId="@Model.UserId"
              method="post">
            <input type="hidden" asp-for="UserId" />
            <input type="hidden" asp-for="UserEmail" />

            @for (int i = 0; i < Model.Roles.Count; i++)
            {
                <div class="form-check mb-2">
                    <input type="hidden" name="Roles[@i].RoleName" value="@Model.Roles[i].RoleName" />
                    <input type="checkbox" name="Roles[@i].IsAssigned" value="true"
                           id="role_@i" class="form-check-input"
                           @(Model.Roles[i].IsAssigned ? "checked" : "") />
                    <input type="hidden" name="Roles[@i].IsAssigned" value="false" />
                    <label class="form-check-label" for="role_@i">@Model.Roles[i].RoleName</label>
                </div>
            }

            <div class="mt-3">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-controller="TenantDashboard" asp-action="Users"
                   asp-route-tenantCode="@tenant.OfficeCode"
                   class="btn btn-outline-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>
