@model AppUser

@{
    var tenant = ViewBag.Tenant as Tenant;
    ViewData["Title"] = $"Delete User — {tenant!.Name}";
}

<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="TenantDashboard" asp-action="Index" asp-route-tenantCode="@tenant.OfficeCode">@tenant.Name</a></li>
        <li class="breadcrumb-item"><a asp-controller="TenantDashboard" asp-action="Users" asp-route-tenantCode="@tenant.OfficeCode">Users</a></li>
        <li class="breadcrumb-item active">Delete User</li>
    </ol>
</nav>

<h1 class="h3 mb-3">Delete User</h1>
<p class="text-danger">Are you sure you want to delete this user?</p>

<dl class="row mb-4">
    <dt class="col-sm-2">Email</dt>
    <dd class="col-sm-10">@Model.Email</dd>

    <dt class="col-sm-2">Tenant</dt>
    <dd class="col-sm-10">@tenant.Name</dd>
</dl>

<form asp-controller="TenantDashboard" asp-action="DeleteUserConfirmed"
      asp-route-tenantCode="@tenant.OfficeCode" asp-route-userId="@Model.Id"
      method="post">
    <input type="submit" value="Delete" class="btn btn-danger" />
    <a asp-controller="TenantDashboard" asp-action="Users"
       asp-route-tenantCode="@tenant.OfficeCode"
       class="btn btn-outline-secondary">Cancel</a>
</form>
