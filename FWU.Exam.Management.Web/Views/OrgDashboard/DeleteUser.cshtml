@model AppUser

@{
    var org = ViewBag.Organization as Organization;
    ViewData["Title"] = $"Delete User — {org!.Name}";
}

<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="OrgDashboard" asp-action="Index" asp-route-officeCode="@org.OfficeCode">@org.Name</a></li>
        <li class="breadcrumb-item"><a asp-controller="OrgDashboard" asp-action="Users" asp-route-officeCode="@org.OfficeCode">Users</a></li>
        <li class="breadcrumb-item active">Delete User</li>
    </ol>
</nav>

<h1 class="h3 mb-3">Delete User</h1>
<p class="text-danger">Are you sure you want to delete this user?</p>

<dl class="row mb-4">
    <dt class="col-sm-2">Email</dt>
    <dd class="col-sm-10">@Model.Email</dd>

    <dt class="col-sm-2">Organization</dt>
    <dd class="col-sm-10">@org.Name</dd>
</dl>

<form asp-controller="OrgDashboard" asp-action="DeleteUserConfirmed"
      asp-route-officeCode="@org.OfficeCode" asp-route-userId="@Model.Id"
      method="post">
    <input type="submit" value="Delete" class="btn btn-danger" />
    <a asp-controller="OrgDashboard" asp-action="Users"
       asp-route-officeCode="@org.OfficeCode"
       class="btn btn-outline-secondary">Cancel</a>
</form>
