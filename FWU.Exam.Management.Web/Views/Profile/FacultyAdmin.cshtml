@model FWU.Exam.Management.Web.ViewModels.FacultyAdminProfileViewModel

@{
    ViewData["Title"] = "My Profile";
}

<div x-data="{ editProfileOpen: false }" class="space-y-6">
    @if (TempData["SuccessMessage"] is string successMsg)
    {
        <div class="flex items-center gap-3 px-4 py-3 rounded-xl bg-green-50 border border-green-200 text-green-700 text-sm" x-data="{ show: true }" x-show="show">
            <i class="fas fa-circle-check"></i>
            <span class="flex-1">@successMsg</span>
            <button type="button" x-on:click="show = false" class="text-green-500 hover:text-green-700"><i class="fas fa-times"></i></button>
        </div>
    }
    @if (TempData["ErrorMessage"] is string errorMsg)
    {
        <div class="flex items-center gap-3 px-4 py-3 rounded-xl bg-red-50 border border-red-200 text-red-700 text-sm" x-data="{ show: true }" x-show="show">
            <i class="fas fa-circle-exclamation"></i>
            <span class="flex-1">@errorMsg</span>
            <button type="button" x-on:click="show = false" class="text-red-500 hover:text-red-700"><i class="fas fa-times"></i></button>
        </div>
    }

    @await Html.PartialAsync("_ProfileHeader", Model)

    <!-- Statistics Cards -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 hover:shadow-md transition-shadow duration-200">
            <div class="p-5">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-xs sm:text-sm font-medium text-gray-500">Colleges</p>
                        <p class="text-xl sm:text-2xl font-bold text-gray-800 mt-1">@Model.CollegeCount</p>
                    </div>
                    <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center">
                        <i class="fas fa-school text-blue-600 text-xl"></i>
                    </div>
                </div>
                <div class="mt-3 flex items-center">
                    <span class="text-xs text-gray-500 font-medium"><i class="fas fa-building-columns mr-1"></i>Affiliated colleges</span>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl shadow-sm border border-gray-200 hover:shadow-md transition-shadow duration-200">
            <div class="p-5">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-xs sm:text-sm font-medium text-gray-500">Programs</p>
                        <p class="text-xl sm:text-2xl font-bold text-gray-800 mt-1">@Model.ProgramCount</p>
                    </div>
                    <div class="w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center">
                        <i class="fas fa-stream text-purple-600 text-xl"></i>
                    </div>
                </div>
                <div class="mt-3 flex items-center">
                    <span class="text-xs text-gray-500 font-medium"><i class="fas fa-book mr-1"></i>Academic programs</span>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl shadow-sm border border-gray-200 hover:shadow-md transition-shadow duration-200">
            <div class="p-5">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-xs sm:text-sm font-medium text-gray-500">Staff</p>
                        <p class="text-xl sm:text-2xl font-bold text-gray-800 mt-1">@Model.StaffCount</p>
                    </div>
                    <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center">
                        <i class="fas fa-users text-green-600 text-xl"></i>
                    </div>
                </div>
                <div class="mt-3 flex items-center">
                    <span class="text-xs text-green-600 font-medium"><i class="fas fa-user-check mr-1"></i>Staff members</span>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl shadow-sm border border-gray-200 hover:shadow-md transition-shadow duration-200">
            <div class="p-5">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-xs sm:text-sm font-medium text-gray-500">Exam Schedules</p>
                        <p class="text-xl sm:text-2xl font-bold text-gray-800 mt-1">@Model.ActiveExamScheduleCount</p>
                    </div>
                    <div class="w-12 h-12 bg-amber-100 rounded-full flex items-center justify-center">
                        <i class="fas fa-clipboard-list text-amber-600 text-xl"></i>
                    </div>
                </div>
                <div class="mt-3 flex items-center">
                    <span class="text-xs text-gray-500 font-medium"><i class="fas fa-calendar-check mr-1"></i>Active schedules</span>
                </div>
            </div>
        </div>
    </div>

    <!-- Details -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="bg-white rounded-xl shadow-sm border border-gray-200">
            <div class="p-6 border-b border-gray-200">
                <h3 class="text-base sm:text-lg font-semibold text-gray-800">Personal Information</h3>
                <p class="text-xs sm:text-sm text-gray-500 mt-1">Basic personal details</p>
            </div>
            <div class="p-6">
                <dl class="grid grid-cols-1 gap-x-8 gap-y-5">
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Full Name</dt>
                        <dd class="text-sm font-semibold text-gray-800 mt-1">@Model.FullName</dd>
                    </div>
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Designation</dt>
                        <dd class="text-sm text-gray-800 mt-1">@(string.IsNullOrEmpty(Model.Designation) ? "—" : Model.Designation)</dd>
                    </div>
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Email</dt>
                        <dd class="text-sm text-gray-800 mt-1">@Model.Email</dd>
                    </div>
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Phone</dt>
                        <dd class="text-sm text-gray-800 mt-1">@(string.IsNullOrEmpty(Model.PhoneNumber) ? "—" : Model.PhoneNumber)</dd>
                    </div>
                </dl>
            </div>
        </div>

        <div class="bg-white rounded-xl shadow-sm border border-gray-200">
            <div class="p-6 border-b border-gray-200">
                <h3 class="text-base sm:text-lg font-semibold text-gray-800">Faculty Information</h3>
                <p class="text-xs sm:text-sm text-gray-500 mt-1">Details of the faculty office</p>
            </div>
            <div class="p-6">
                <dl class="grid grid-cols-1 gap-x-8 gap-y-5">
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Faculty</dt>
                        <dd class="text-sm font-semibold text-gray-800 mt-1">@(string.IsNullOrEmpty(Model.FacultyName) ? "—" : Model.FacultyName)</dd>
                    </div>
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Short Name</dt>
                        <dd class="text-sm text-gray-800 mt-1">@(string.IsNullOrEmpty(Model.FacultyShortName) ? "—" : Model.FacultyShortName)</dd>
                    </div>
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Office Code</dt>
                        <dd class="text-sm text-gray-800 mt-1">@(string.IsNullOrEmpty(Model.OfficeCode) ? "—" : Model.OfficeCode)</dd>
                    </div>
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Email</dt>
                        <dd class="text-sm text-gray-800 mt-1">@(string.IsNullOrEmpty(Model.FacultyEmail) ? "—" : Model.FacultyEmail)</dd>
                    </div>
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Contact</dt>
                        <dd class="text-sm text-gray-800 mt-1">@(string.IsNullOrEmpty(Model.FacultyContactNumber) ? "—" : Model.FacultyContactNumber)</dd>
                    </div>
                    <div>
                        <dt class="text-xs font-medium text-gray-500 uppercase tracking-wide">Address</dt>
                        <dd class="text-sm text-gray-800 mt-1">@(string.IsNullOrEmpty(Model.FacultyAddress) ? "—" : Model.FacultyAddress)</dd>
                    </div>
                </dl>
            </div>
        </div>
    </div>
</div>
