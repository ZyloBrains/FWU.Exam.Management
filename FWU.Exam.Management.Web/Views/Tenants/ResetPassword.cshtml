@model FWU.Exam.Management.Web.ViewModels.TenantResetPasswordViewModel

@{
    ViewData["Title"] = "Reset Tenant Password";
}

<div class="space-y-6">
    <div class="flex items-center gap-3">
        <a asp-action="Index" class="w-10 h-10 bg-gray-100 rounded-lg flex items-center justify-center hover:bg-gray-200 transition">
            <i class="fas fa-arrow-left text-gray-600"></i>
        </a>
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center">
                <i class="fas fa-key text-red-600"></i>
            </div>
            <div>
                <h1 class="text-xl sm:text-2xl font-bold text-gray-800">Reset Password</h1>
                <p class="text-xs sm:text-sm text-gray-500">for <span class="font-medium text-gray-700">@Model.TenantName</span> (@Model.OfficeCode)</p>
            </div>
        </div>
    </div>

    <div class="bg-white rounded-xl shadow-lg border border-gray-300 p-8">
        <form asp-action="ResetPassword" method="post" class="space-y-6">
            <input type="hidden" asp-for="TenantId" />
            <input type="hidden" asp-for="TenantName" />
            <input type="hidden" asp-for="OfficeCode" />
            <input type="hidden" asp-for="AdminEmail" />

            <div asp-validation-summary="ModelOnly" class="text-sm text-red-600 bg-red-50 rounded-lg p-2"></div>

            <div class="bg-gray-50 rounded-lg p-4 border border-gray-200">
                <div class="flex items-center gap-3">
                    <div class="w-10 h-10 bg-indigo-100 rounded-full flex items-center justify-center">
                        <i class="fas fa-user text-indigo-600"></i>
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-800">@Model.TenantName</p>
                        <p class="text-xs text-gray-500">@Model.AdminEmail</p>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                <div>
                    <label asp-for="NewPassword" class="text-xs sm:text-sm font-medium text-gray-700 block mb-1">New Password</label>
                    <div class="relative flex">
                        <input asp-for="NewPassword" id="NewPasswordInput" type="password"
                               class="w-full border border-gray-300 rounded-l-md px-3 py-2 text-sm focus:ring-2 focus:ring-red-500 focus:border-red-500" />
                        <button type="button" id="toggleNewPassword"
                                class="px-2.5 bg-gray-100 border border-l-0 border-gray-300 text-gray-500 hover:text-gray-700 hover:bg-gray-200 transition text-sm">
                            <i class="fas fa-eye" id="toggleNewPasswordIcon"></i>
                        </button>
                        <button type="button" id="generatePasswordBtn"
                                class="px-3 bg-indigo-100 text-indigo-700 text-xs font-semibold border border-l-0 border-gray-300 rounded-r-md hover:bg-indigo-200 transition flex items-center gap-1">
                            <i class="fas fa-dice"></i> Generate
                        </button>
                    </div>
                    <span asp-validation-for="NewPassword" class="text-xs text-red-500 mt-1 block"></span>
                </div>
                <div>
                    <label asp-for="ConfirmPassword" class="text-xs sm:text-sm font-medium text-gray-700 block mb-1">Confirm Password</label>
                    <div class="relative flex">
                        <input asp-for="ConfirmPassword" id="ConfirmPasswordInput" type="password"
                               class="w-full border border-gray-300 rounded-l-md px-3 py-2 text-sm focus:ring-2 focus:ring-red-500 focus:border-red-500" />
                        <button type="button" id="toggleConfirmPassword"
                                class="px-2.5 bg-gray-100 border border-l-0 border-gray-300 rounded-r-md text-gray-500 hover:text-gray-700 hover:bg-gray-200 transition text-sm">
                            <i class="fas fa-eye" id="toggleConfirmPasswordIcon"></i>
                        </button>
                    </div>
                    <span asp-validation-for="ConfirmPassword" class="text-xs text-red-500 mt-1 block"></span>
                </div>
            </div>

            <div class="bg-amber-50 border border-amber-200 rounded-lg p-3">
                <div class="flex items-start gap-2">
                    <i class="fas fa-exclamation-triangle text-amber-500 mt-0.5"></i>
                    <p class="text-xs sm:text-sm text-amber-700">The admin user will be required to use this new password on their next login.</p>
                </div>
            </div>

            <div class="flex items-center gap-4 pt-4 border-t border-gray-200">
                <button type="submit" class="px-6 py-2 bg-red-500 text-white text-sm font-semibold rounded-lg hover:bg-red-600 transition shadow-md">
                    <i class="fas fa-key mr-1"></i> Reset Password
                </button>
                <a asp-action="Index" class="px-6 py-2 text-sm font-semibold text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 transition shadow-md">
                    Cancel
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $(function () {
            function togglePassword(inputId, iconId) {
                var $input = $(inputId);
                var $icon = $(iconId);
                if ($input.attr('type') === 'password') {
                    $input.attr('type', 'text');
                    $icon.removeClass('fa-eye').addClass('fa-eye-slash');
                } else {
                    $input.attr('type', 'password');
                    $icon.removeClass('fa-eye-slash').addClass('fa-eye');
                }
            }

            $('#toggleNewPassword').on('click', function () {
                togglePassword('#NewPasswordInput', '#toggleNewPasswordIcon');
            });

            $('#toggleConfirmPassword').on('click', function () {
                togglePassword('#ConfirmPasswordInput', '#toggleConfirmPasswordIcon');
            });

            $('#generatePasswordBtn').on('click', function () {
                var length = 14;
                var lower = 'abcdefghijklmnopqrstuvwxyz';
                var upper = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
                var digits = '0123456789';
                var special = '@@#$!%*?&';
                var all = lower + upper + digits + special;

                function getRandomChar(str) {
                    var array = new Uint32Array(1);
                    window.crypto.getRandomValues(array);
                    return str[array[0] % str.length];
                }

                function shuffle(arr) {
                    for (var i = arr.length - 1; i > 0; i--) {
                        var array = new Uint32Array(1);
                        window.crypto.getRandomValues(array);
                        var j = array[0] % (i + 1);
                        var tmp = arr[i];
                        arr[i] = arr[j];
                        arr[j] = tmp;
                    }
                }

                var pw = [];
                pw.push(getRandomChar(lower));
                pw.push(getRandomChar(upper));
                pw.push(getRandomChar(digits));
                pw.push(getRandomChar(special));

                for (var i = 4; i < length; i++) {
                    pw.push(getRandomChar(all));
                }

                shuffle(pw);
                var password = pw.join('');

                $('#NewPasswordInput').val(password).attr('type', 'text');
                $('#toggleNewPasswordIcon').removeClass('fa-eye').addClass('fa-eye-slash');
                $('#ConfirmPasswordInput').val(password).attr('type', 'text');
                $('#toggleConfirmPasswordIcon').removeClass('fa-eye').addClass('fa-eye-slash');
            });
        });
    </script>
}
